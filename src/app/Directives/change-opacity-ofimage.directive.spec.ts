import { ChangeOpacityOFImageDirective } from './change-opacity-ofimage.directive';

describe('ChangeOpacityOFImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeOpacityOFImageDirective();
    expect(directive).toBeTruthy();
  });
});

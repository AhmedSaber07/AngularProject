import { QuantityStatePipe } from './quantity-state.pipe';

describe('QuantityStatePipe', () => {
  it('create an instance', () => {
    const pipe = new QuantityStatePipe();
    expect(pipe).toBeTruthy();
  });
});
